{
  "buildCommand": "rm -rf node_modules package-lock.json pnpm-lock.yaml yarn.lock && npm install && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install --force",
  "framework": "nextjs"
}
